{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": [
        "apps/web/.next/**",
        "packages/**/dist/**"
      ]
    },
    "dev": {
      "cache": false
    }
  }
}